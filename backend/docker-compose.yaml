version: "3.8"
services:
  postgres:
    image: postgres:16
    container_name: edukinder_postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      - .env
    volumes:
      - ./pgdata:/var/lib/postgresql/data